# This module is used for testing by conda when setting CIO_TEST
import os

level = int(os.getenv('CIO_TEST') or 0)
base_urls = []
if level >= 1:
    base_urls.extend(['http://filer/pkgs/pro',
                      'http://filer/pkgs/free'])
if level >= 2:
    base_urls.insert(0, 'http://filer/test-pkgs')
if level == 3:
    base_urls.insert(0, 'http://filer/test-pkgs-%s' % os.getenv('USER'))
